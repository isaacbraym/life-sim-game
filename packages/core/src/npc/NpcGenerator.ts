import type { Npc } from '../schemas/npc';
import type { SelectorNpc } from '../schemas/event';

// TODO: implementar geração procedural de NPC dentro das constraints
export function gerarNpcNovo(_seletor: SelectorNpc, _semente: number): Npc {
  throw new Error('not implemented');
}
